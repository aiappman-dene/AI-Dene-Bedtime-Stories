# DreamTalez TODO

- Split large frontend surfaces out of public/app.js into smaller modules.
- Replace inline event handlers so CSP can drop unsafe-inline over time.
- Add a focused smoke test for /generate plus /api/job polling.
- Revisit multi-instance readiness and remove leftover in-memory request guards.
- Review mobile runtime configuration before the next Android release.
